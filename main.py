import datetime
import numpy
import os
import sys
import time
import utility

if __name__ == "__main__":
    # execute only if run as a script
    print(utility.getFlightTime())
    # if (utility.getFailureVector() is not None):
        # if (np.random.normal())
